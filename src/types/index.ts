export type OnBoardingData = {
  id: number;
  title: string;
  description: string;
  icon: string;
};

export type TextStyles = {
  fontSize?: number;
  fontFamily?: string;
  color?: string;
  fontWeight?: string | number;
  textAlign?: string;
};
